

import 'package:flutter/material.dart';

class OtpAuthenticationPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return (
      Container(child: Text('OTP Authentication Page'))
    );
  }
}